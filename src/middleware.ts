// Middleware is removed since we're using client-side locale switching
// instead of route-based internationalization

export const config = {
  // No specific matcher needed for internationalization
  matcher: []
};